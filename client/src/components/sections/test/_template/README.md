# Copy me

Do **not** register this folder in `section-manifest.ts` or the CMS catalog.

```bash
cp -R _template my-new-section
# Then rename TemplateBand → MyNewSection across files
```

Add a preview page at `app/(public)/user-guide/test/my-new-section/page.tsx`.
