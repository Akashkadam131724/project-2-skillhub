import type { SiteBuilderHeroUiProps } from "./types";
import { DEMO_IMAGES } from "@/lib/demo/shared-demo-images";

export const SITE_BUILDER_HERO_STATIC_DEMO: SiteBuilderHeroUiProps = {
  badge: "SkillHub Studio",
  title: "Design learning destinations that convert",
  subtitle:
    "Pick a section, drop it on the page, edit copy on the live URL—no deploy required.",
  body:
    "<p>From hero bands to pricing tables, every block is CMS-ready and brand-aware.</p>",
  imageUrl: DEMO_IMAGES.heroOffice,
};
