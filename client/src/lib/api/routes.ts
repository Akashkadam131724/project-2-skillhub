/**
 * Typed re-export of the API route catalog (`api-routes.mjs`).
 * Import `API` from here in TypeScript; Postman imports the `.mjs` directly.
 */
export { API, enc, withQuery } from "./api-routes.mjs";
export { default } from "./api-routes.mjs";
