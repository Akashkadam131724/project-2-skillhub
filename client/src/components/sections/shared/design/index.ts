/**
 * Section layout primitives — NOT CMS page sections.
 *
 * Themed surfaces (cards, overlays) for composing *Ui components.
 * Tokens live in `@/lib/sections/section-design-system`.
 */
export { default as SectionLightCard } from "./SectionLightCard";
export { default as SectionMediaOverlay } from "./SectionMediaOverlay";
